[pytest]
testpaths = tests
pythonpath = .
addopts = -m "not external_gt"
markers =
    algorithm: fast tests for pure algorithm behavior
    external_gt: tests that require local GT/video/API assets outside the repo
